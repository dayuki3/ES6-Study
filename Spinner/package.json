{
  "dependencies": {
    "@babel/polyfill": "^7.0.0",
    "babel-preset-env": "^1.7.0",
    "jest": "^23.6.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.1.5",
    "@babel/core": "^7.1.6",
    "@babel/plugin-transform-modules-umd": "^7.2.0",
    "@babel/preset-env": "^7.1.6",
    "babel-core": "7.0.0-bridge.0",
    "babel-jest": "^23.6.0"
  },
  "scripts": {
    "test": "export NODE_ENV=test && jest",
    "watch": "export NODE_ENV=test && jest --watch",
    "build": "export NODE_ENV=product && ./node_modules/.bin/babel js -d dist"
  }
}
